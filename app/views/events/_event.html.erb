<!-- app/views/events/_event.html.erb -->

<div class="event d-flex justify-content-between align-items-center mb-3">
  <h2> #<%= event.id %> <%= event.name.titleize %></h2>
   <%= button_to 'Delete', event, method: :delete, remote: true, class: 'btn btn-danger' %>
</div>
